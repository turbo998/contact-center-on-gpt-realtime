# Frontend (Next.js 14 + TypeScript)

See `docs/14-frontend-design.md` for the full design.

## Run locally

```bash
npm install
cp ../.env.example ../.env  # then export NEXT_PUBLIC_* vars
npm run dev
```

Open http://localhost:3000.
